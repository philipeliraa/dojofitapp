package com.dojofit.api.model.enums;

/**
 * Cores de faixa (docs/03 §3). Sistema de cor separado da marca: presa aos 5
 * tokens belt.* documentados — nenhuma cor de faixa fora destes. Faixas
 * infantis exigiriam novos tokens (decisão de docs/03), fora do escopo da 3a.
 */
public enum CorFaixa {
    BRANCA, AZUL, ROXA, MARROM, PRETA
}
